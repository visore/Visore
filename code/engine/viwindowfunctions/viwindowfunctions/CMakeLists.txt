#Setup directory paths
SET(ROOT ${CMAKE_CURRENT_SOURCE_DIR})
SET(VI_NOWINDOWFUNCTION ${ROOT}/vinowindowfunction)
SET(VI_HANNWINDOWFUNCTION ${ROOT}/vihannwindowfunction)

#Initiate sub-directories
ADD_SUBDIRECTORY(${VI_NOWINDOWFUNCTION})
ADD_SUBDIRECTORY(${VI_HANNWINDOWFUNCTION})
