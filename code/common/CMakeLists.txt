#Setup directory paths
SET(ROOT ${CMAKE_CURRENT_SOURCE_DIR})
SET(LOGOS ${ROOT}/logos)

#Install file
IF(${SYSTEM} STREQUAL "MACOSX")
	INSTALL(FILES ${LOGOS}/logo.icns DESTINATION ../Resources)
ELSE()
	INSTALL(FILES ${LOGOS}/logo.png DESTINATION .)
ENDIF()
