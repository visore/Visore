#Setup directory paths
SET(ROOT ${CMAKE_CURRENT_SOURCE_DIR})
SET(FORMS ${ROOT}/forms)
SET(HEADERS ${ROOT}/headers)
SET(SOURCES ${ROOT}/sources)

#Initiate source files
FILE(GLOB VI_MAINWINDOW_FORMS ${FORMS}/*.ui)
FILE(GLOB VI_MAINWINDOW_SOURCES ${SOURCES}/*.cpp)
FILE(GLOB VI_MAINWINDOW_HEADERS ${HEADERS}/*.h)

#Create library
QT5_WRAP_UI(VI_WRAPPER_FORMS ${VI_MAINWINDOW_FORMS})
ADD_LIBRARY(vimainwindow SHARED ${VI_MAINWINDOW_HEADERS} ${VI_MAINWINDOW_SOURCES} ${VI_WRAPPER_FORMS})
TARGET_LINK_LIBRARIES(vimainwindow vicomposedwidgets)

#Install file
INSTALL(TARGETS vimainwindow DESTINATION interface)
