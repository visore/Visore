#Setup directory paths
SET(ROOT ${CMAKE_CURRENT_SOURCE_DIR})
SET(HEADERS ${ROOT}/headers)
SET(SOURCES ${ROOT}/sources)
SET(RESOURCES ${ROOT}/resources)
FILE(GLOB VI_ORANGETHEME_RESOURCES ${RESOURCES}/*.qrc)

#Initiate source files
FILE(GLOB VI_THEME_SOURCES ${SOURCES}/*.cpp)
FILE(GLOB VI_THEME_HEADERS ${HEADERS}/*.h)

#Wrap Qt files
QT5_ADD_RESOURCES(VI_WRAPPER_RESOURCES ${VI_ORANGETHEME_RESOURCES})

#Create library
ADD_LIBRARY(vitheme SHARED ${VI_THEME_HEADERS} ${VI_THEME_SOURCES} ${VI_WRAPPER_RESOURCES})
TARGET_LINK_LIBRARIES(vitheme ${QT_LIBRARIES} viessentials)

#Install file
INSTALL(TARGETS vitheme DESTINATION interface)
